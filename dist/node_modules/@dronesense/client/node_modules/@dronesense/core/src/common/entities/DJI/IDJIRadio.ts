export interface IDJIRadio {
    // The signal strength of the remote connection. (uint8_t)
    remoteSignalPercent: number;

    // The signal strength of the video connection.
    videoSignalPercent: number;
}

export default IDJIRadio;
