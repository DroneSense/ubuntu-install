import { IEventEmitter } from './IEventEmitter';
import { IGimbalState } from './entities/IGimbalState';
export declare module constants {
    module commands {
    }
    module events {
        const STATE_UPDATED: string;
    }
}
export interface IGimbal extends IEventEmitter {
    State: IGimbalState;
    on(eventName: string, callback?: Function, context?: any): any;
    on(eventName: 'state-updated', callback?: (state: IGimbalState) => void, context?: any): any;
}
export default IGimbal;
