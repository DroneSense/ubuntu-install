import { FlightPlan } from '@dronesense/model';
export declare module constants {
    module commands {
        const CLEAR_FLIGHT_PLAN: string;
        const GET_FLIGHT_PLAN: string;
        const SET_FLIGHT_PLAN: string;
        const START_FLIGHT_PLAN: string;
    }
}
export interface IFlightPlanner {
    clearFlightPlan(): Promise<void>;
    getFlightPlan(): Promise<FlightPlan>;
    setFlightPlan(flightPlan: FlightPlan): Promise<void>;
    startFlightPlan(): Promise<void>;
}
export default IFlightPlanner;
