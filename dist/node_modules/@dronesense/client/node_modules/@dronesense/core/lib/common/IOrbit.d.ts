import { IEventEmitter } from './IEventEmitter';
export declare module constants {
    module commands {
        const ORBIT: string;
    }
}
export interface IOrbit extends IEventEmitter {
    orbit(lattitude: number, longitude: number, altitude: number, radius: number, isClockwise: boolean, angularVelocity: number): Promise<void>;
}
export default IOrbit;
