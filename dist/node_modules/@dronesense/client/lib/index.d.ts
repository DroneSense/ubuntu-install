import { IDroneSenseService } from '@dronesense/core/lib/common/IDroneSenseService';
export interface IClient {
    createClient(address: string): IDroneSenseService;
}
declare var DS: IClient;
export default DS;
