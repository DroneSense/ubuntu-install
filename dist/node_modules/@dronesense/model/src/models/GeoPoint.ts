// Basic type for a location
export class GeoPoint {

    constructor(latitude: number, longitude: number) {
        this.lat = latitude;
        this.lng = longitude;
    }

    // In decimal
    lat: number;

    // In decimal
    lng: number;

}

export default GeoPoint;
