import Command from '../Command';
import GeoPoint from '../GeoPoint';
export declare class WaypointCommand extends Command {
    isBase: boolean;
    constructor(type: string, id: string, handle: any, isBase: boolean);
    Position: GeoPoint;
    Altitude: number;
    AltitudeMSL: number;
    AltitudeHAE: number;
    GroundElevationMSL: number;
    GroundElevationHAE: number;
    FlightSpeed: number;
    Heading: number;
    CameraAngle: number;
    hydrate(data: any): boolean;
}
