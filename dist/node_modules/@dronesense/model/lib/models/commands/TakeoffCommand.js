"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var WaypointCommand_1 = require('./WaypointCommand');
var TakeoffCommand = (function (_super) {
    __extends(TakeoffCommand, _super);
    function TakeoffCommand(type, id, handle, isBase) {
        _super.call(this, type, id, handle, true);
        this.isBase = isBase;
        if (this.handle) {
            this.hydrate(this.handle.data);
            if (!isBase) {
                this.handle.on(this.handle.EVENTS.DATA_CHANGED, this.hydrate.bind(this));
            }
        }
    }
    TakeoffCommand.prototype.hydrate = function (data) {
        if (_.isEmpty(data)) {
            this.handle.off(this.handle.EVENTS.DATA_CHANGED);
            return;
        }
        if (data) {
            var dataChanged = false;
            dataChanged = _super.prototype.hydrate.call(this, data);
            if (this.ClimbSpeed !== data.ClimbSpeed) {
                this.ClimbSpeed = data.ClimbSpeed;
                dataChanged = true;
            }
            if (dataChanged && !this.isBase) {
            }
            return dataChanged;
        }
    };
    return TakeoffCommand;
}(WaypointCommand_1.WaypointCommand));
exports.TakeoffCommand = TakeoffCommand;

//# sourceMappingURL=TakeoffCommand.js.map
