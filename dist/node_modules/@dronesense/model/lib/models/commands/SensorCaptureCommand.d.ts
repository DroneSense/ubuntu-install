import { WaypointCommand } from './WaypointCommand';
export declare class SensorCaptureCommand extends WaypointCommand {
    isBase: boolean;
    constructor(type: string, id: string, handle: any, isBase: boolean);
    Heading: number;
    Pitch: number;
    Roll: number;
    GSD: number;
    hydrate(data: any): boolean;
}
